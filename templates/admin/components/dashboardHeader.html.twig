<header class="bg-white shadow-sm sticky top-0 z-50">
        <div class="container mx-auto px-4 py-4 flex flex-col sm:flex-row justify-between items-center">
            <div class="text-xl font-bold text-gray-900 mb-4 sm:mb-0">
                <a href="{{ path('app_dashboard') }}">
                    <img src="{{ asset('images/logo.png') }}" alt="Sabre Armory Logo" class="h-24 w-auto mr-3">
                </a>            
            </div>
            <nav>
                <ul class="flex flex-wrap justify-center sm:space-x-4 space-x-2">
                    <li><a href="{{ path('app_inventory') }}" class="px-4 py-2 rounded-lg font-medium text-gray-700 hover:bg-gray-100 transition">Inventory</a></li>
                    <li><a href="{{ path('app_request') }}" class="px-4 py-2 rounded-lg font-medium text-gray-700 hover:bg-gray-100 transition">Requests</a></li>
                    <li><a href="{{ path('app_report') }}" class="px-4 py-2 rounded-lg font-medium text-gray-700 hover:bg-gray-100 transition">Reports</a></li>
                    <li><a href="{{ path('app_admin') }}" class="px-4 py-2 rounded-lg font-medium text-gray-700 hover:bg-gray-100 transition">Admin</a></li>
                    <li><a href="{{ path('app_alert') }}" class="px-4 py-2 rounded-lg font-medium text-gray-700 hover:bg-gray-100 transition">Alerts</a></li>
                </ul>
            </nav>
        </div>
</header>