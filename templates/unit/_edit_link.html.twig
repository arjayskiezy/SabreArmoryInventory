{% if unit is defined %}
    <a
        href="{{ path('app_unit_edit', { id: unit.id }) }}"
        @click.prevent="loadModal('{{ path('app_unit_edit', { id: unit.id }) }}')"
        class="text-green-600 hover:underline"
    >
        Edit
    </a>
{% endif %}
