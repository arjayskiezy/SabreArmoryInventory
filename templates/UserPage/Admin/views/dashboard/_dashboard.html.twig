<!-- Dashboard view -->
            <section x-show="view === 'dashboard'">
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-6">
                    <!-- widget: pending requests -->
                    <div class="bg-white rounded shadow p-4">
                        <div class="flex items-center justify-between">
                            <div>
                                <div class="text-xs text-gray-500">
                                    Pending Requests
                                </div>
                                <div class="text-2xl font-bold">7</div>
                            </div>
                            <div class="text-sm text-gray-400">
                                Review & Approve
                            </div>
                        </div>
                        <div class="mt-4 text-sm text-gray-600">
                            <button
                                class="px-3 py-1 rounded bg-accent text-white"
                                @click="setView('requests')"
                            >
                                Open Requests
                            </button>
                        </div>
                    </div>

                    <!-- widget: low ammo alerts -->
                    <div class="bg-white rounded shadow p-4">
                        <div class="text-xs text-gray-500">Low Ammo Alerts</div>
                        <div class="mt-2">
                            <ul class="space-y-2 text-sm">
                                <li class="flex justify-between items-center">
                                    <div>
                                        9mm Rounds
                                        <span class="ml-2 text-xs text-gray-400"
                                            >Supplier: Alpha</span
                                        >
                                    </div>
                                    <div class="text-red-600 font-semibold">
                                        12 left
                                    </div>
                                </li>
                                <li class="flex justify-between items-center">
                                    <div>5.56mm Rounds</div>
                                    <div class="text-red-600 font-semibold">
                                        20 left
                                    </div>
                                </li>
                            </ul>
                        </div>
                    </div>

                    <!-- widget: audit summary -->
                    <div class="bg-white rounded shadow p-4">
                        <div class="text-xs text-gray-500">
                            Recent Audit Activity
                        </div>
                        <div class="mt-2 text-sm">
                            <ul class="space-y-1">
                                <li>Officer Smith issued M4-123 (2 hrs ago)</li>
                                <li>
                                    Maintenance logged for Pistol-45 (1 day ago)
                                </li>
                            </ul>
                        </div>
                        <div class="mt-4">
                            <button
                                class="px-3 py-1 rounded border"
                                @click="setView('audit')"
                            >
                                View Logs
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Analytics / Charts area (placeholders) -->
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                    <div class="bg-white rounded shadow p-4">
                        <div class="flex items-center justify-between mb-3">
                            <div class="font-semibold">Issuance Over Time</div>
                            <div class="text-xs text-gray-400">
                                Last 30 days
                            </div>
                        </div>
                        <!-- Insert your charting library canvas here (Chart.js, ApexCharts) -->
                        <div
                            class="h-48 flex items-center justify-center text-gray-400"
                        >
                            [Chart placeholder]
                        </div>
                    </div>

                    <div class="bg-white rounded shadow p-4">
                        <div class="flex items-center justify-between mb-3">
                            <div class="font-semibold">Top Used Weapons</div>
                            <div class="text-xs text-gray-400">By issues</div>
                        </div>
                        <div class="space-y-2 text-sm">
                            <div class="flex justify-between">
                                <div>M4A1</div>
                                <div>342</div>
                            </div>
                            <div class="flex justify-between">
                                <div>Pistol-45</div>
                                <div>198</div>
                            </div>
                            <div class="flex justify-between">
                                <div>Sabre-1</div>
                                <div>30</div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>