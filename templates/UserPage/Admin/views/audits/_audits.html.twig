{% extends 'UserPage/Admin/index.html.twig' %} {% block title %}Audit Logs{%
endblock %} {% block admin_content %}
<div class="space-y-6">
    <!-- Header -->
    <div class="flex items-center justify-between">
        <div>
            <h2 class="text-xl font-semibold">Audit Logs</h2>
            <p class="text-sm text-gray-500">
                Full activity trail and system history
            </p>
        </div>

        <!-- Filters & Export -->
        <div class="flex items-center gap-2">
            <input
                x-model="filter.q"
                @input.debounce.300="searchLogs"
                type="text"
                placeholder="Search user, item, or action..."
                class="px-3 py-2 border rounded-xl text-sm border border-gray-200"
            />
            <button
                @click="openModal('filterLogs')"
                class="bg-tertiary text-white text-sm font-medium px-4 py-2 rounded-lg shadow transition-transform transform hover:scale-105"
            >
                Filters
            </button>
            <button
                @click="openModal('exportLogs')"
                class="bg-primary text-white text-sm font-medium px-4 py-2 rounded-lg shadow transition-transform transform hover:scale-105"
            >
                Export
            </button>
        </div>
    </div>

    <!-- Logs Container -->
    <div class="bg-white rounded-xl shadow overflow-hidden">
        <ul class="divide-y divide-gray-100">
            <!-- Example log item — replace with Twig loop -->
            <li
                class="p-4 flex justify-between items-start hover:bg-gray-50 transition-colors"
            >
                <div>
                    <div class="text-sm font-medium text-gray-800">
                        Issue recorded:
                        <span class="font-semibold">M4-12345</span> → Officer
                        John
                    </div>
                    <div class="text-xs text-gray-500 mt-1">
                        By Officer Smith —
                        <span class="font-mono">2025-09-24 14:22</span>
                    </div>
                </div>
                <div class="text-xs text-gray-400 font-mono">
                    Action ID: A-00012
                </div>
            </li>

            <li
                class="p-4 flex justify-between items-start hover:bg-gray-50 transition-colors"
            >
                <div>
                    <div class="text-sm font-medium text-gray-800">
                        Return logged:
                        <span class="font-semibold">Pistol-9982</span> from
                        Officer Jane
                    </div>
                    <div class="text-xs text-gray-500 mt-1">
                        By Admin —
                        <span class="font-mono">2025-09-25 10:12</span>
                    </div>
                </div>
                <div class="text-xs text-gray-400 font-mono">
                    Action ID: A-00013
                </div>
            </li>

            <li
                class="p-4 flex justify-between items-start hover:bg-gray-50 transition-colors"
            >
                <div>
                    <div class="text-sm font-medium text-gray-800">
                        Issue recorded:
                        <span class="font-semibold">M4-12345</span> → Officer
                        John
                    </div>
                    <div class="text-xs text-gray-500 mt-1">
                        By Officer Smith —
                        <span class="font-mono">2025-09-24 14:22</span>
                    </div>
                </div>
                <div class="text-xs text-gray-400 font-mono">
                    Action ID: A-00012
                </div>
            </li>

            <li
                class="p-4 flex justify-between items-start hover:bg-gray-50 transition-colors"
            >
                <div>
                    <div class="text-sm font-medium text-gray-800">
                        Issue recorded:
                        <span class="font-semibold">M4-12345</span> → Officer
                        John
                    </div>
                    <div class="text-xs text-gray-500 mt-1">
                        By Officer Smith —
                        <span class="font-mono">2025-09-24 14:22</span>
                    </div>
                </div>
                <div class="text-xs text-gray-400 font-mono">
                    Action ID: A-00012
                </div>
            </li>

            <li
                class="p-4 flex justify-between items-start hover:bg-gray-50 transition-colors"
            >
                <div>
                    <div class="text-sm font-medium text-gray-800">
                        Issue recorded:
                        <span class="font-semibold">M4-12345</span> → Officer
                        John
                    </div>
                    <div class="text-xs text-gray-500 mt-1">
                        By Officer Smith —
                        <span class="font-mono">2025-09-24 14:22</span>
                    </div>
                </div>
                <div class="text-xs text-gray-400 font-mono">
                    Action ID: A-00012
                </div>
            </li>

            <li
                class="p-4 flex justify-between items-start hover:bg-gray-50 transition-colors"
            >
                <div>
                    <div class="text-sm font-medium text-gray-800">
                        Issue recorded:
                        <span class="font-semibold">M4-12345</span> → Officer
                        John
                    </div>
                    <div class="text-xs text-gray-500 mt-1">
                        By Officer Smith —
                        <span class="font-mono">2025-09-24 14:22</span>
                    </div>
                </div>
                <div class="text-xs text-gray-400 font-mono">
                    Action ID: A-00012
                </div>
            </li>

            <li
                class="p-4 flex justify-between items-start hover:bg-gray-50 transition-colors"
            >
                <div>
                    <div class="text-sm font-medium text-gray-800">
                        Issue recorded:
                        <span class="font-semibold">M4-12345</span> → Officer
                        John
                    </div>
                    <div class="text-xs text-gray-500 mt-1">
                        By Officer Smith —
                        <span class="font-mono">2025-09-24 14:22</span>
                    </div>
                </div>
                <div class="text-xs text-gray-400 font-mono">
                    Action ID: A-00012
                </div>
            </li>

            <!-- More logs here -->
        </ul>
    </div>

    <!-- Pagination or load more -->
    <div class="flex justify-center mt-4">
        <button
            @click="loadMoreLogs"
            class="bg-secondary text-white text-sm font-medium px-4 py-2 rounded-lg shadow transition-transform transform hover:scale-105"
        >
            Load More
        </button>
    </div>
</div>

{% endblock %}
