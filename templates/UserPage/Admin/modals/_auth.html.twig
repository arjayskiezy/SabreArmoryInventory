<div x-show="modal.name === 'authorize'">
            <div class="text-sm text-gray-700 mb-4">
                Authorize Request
                <span class="font-medium" x-text="modal.data.reqId"></span>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                <div>
                    <label class="text-sm text-gray-600">Authorizer Name</label>
                    <input
                        x-model="modal.data.authName"
                        class="mt-1 px-3 py-2 border rounded w-full"
                    />
                </div>
                <div>
                    <label class="text-sm text-gray-600">Passcode / PIN</label>
                    <input
                        x-model="modal.data.authPin"
                        type="password"
                        class="mt-1 px-3 py-2 border rounded w-full"
                    />
                </div>
            </div>

            <div class="mt-4 text-sm text-gray-500">
                Digital signature placeholder — integrate with your signing or
                SSO provider. After authorizer confirmation, the issuance is
                logged in Audit Logs.
            </div>

            <div class="mt-4 flex justify-end gap-2">
                <button class="px-4 py-2 border rounded" @click="closeModal()">
                    Cancel
                </button>
                <button
                    class="px-4 py-2 bg-accent text-white rounded"
                    @click="confirmAuthorization()"
                >
                    Authorize
                </button>
            </div>
        </div>