{% if user is defined %}
    <a
        href="{{ path('app_user_roles_edit', { id: user.id }) }}"
        @click.prevent="loadModal('{{ path('app_user_roles_edit', { id: user.id }) }}')"
        class="px-4 py-2 bg-green-600 hover:bg-green-700 text-white text-xs font-medium px-3 py-1 rounded-lg shadow inline-block"
    >
        Edit
    </a>
{% endif %}
