<button
    @click="loadModal('{{ path('app_unit_show', { id: unit.id }) }}')"
    class="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-xs font-medium px-3 py-1 rounded-lg shadow"
>
    Show
</button>
