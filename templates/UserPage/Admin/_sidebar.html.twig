<!-- Off-canvas mobile sidebar -->
<div class="md:hidden" x-show="mobileSidebar" x-cloak>
    <div
        class="fixed inset-0 z-30"
        @click="mobileSidebar = false"
        style="background: rgba(2, 6, 23, 0.5)"
    ></div>
    <aside
        class="fixed left-0 top-0 z-40 w-72 h-full bg-sidebar text-white p-4 overflow-y-auto"
    >
        <div class="flex items-center justify-between mb-6">
            <img
                src="{{ asset('images/logo-light.png') }}"
                alt="Sabre Armory Logo"
                class="h-24 w-auto mr-3"
            />
            <button @click="mobileSidebar = false" class="text-gray-300">
                ✕
            </button>
        </div>

        <!-- mobile nav -->
        <nav class="space-y-1 text-sm">
            <a
                href="#"
                @click="setView('dashboard')"
                class="block px-3 py-2 rounded hover:bg-slate-700"
                >Dashboard</a
            >
            <a
                href="#"
                @click="setView('catalog')"
                class="block px-3 py-2 rounded hover:bg-slate-700"
                >Catalog</a
            >
            <a
                href="#"
                @click="setView('requests')"
                class="block px-3 py-2 rounded hover:bg-slate-700"
                >Requests</a
            >
            <a
                href="#"
                @click="setView('inventory')"
                class="block px-3 py-2 rounded hover:bg-slate-700"
                >Inventory</a
            >
            <a
                href="#"
                @click="setView('users')"
                class="block px-3 py-2 rounded hover:bg-slate-700"
                >Users</a
            >
            <a
                href="#"
                @click="setView('reports')"
                class="block px-3 py-2 rounded hover:bg-slate-700"
                >Reports</a
            >
            <a
                href="#"
                @click="setView('audit')"
                class="block px-3 py-2 rounded hover:bg-slate-700"
                >Audit Logs</a
            >
        </nav>
    </aside>
</div>

<aside
    class="hidden md:flex md:flex-col w-55 h-screen bg-gray-900 text-white p-6 sticky top-0"
>
    <!-- Logo / Brand -->
    <div class="flex items-center gap-3 mb-8">
        <img
            src="{{ asset('images/logo-light.png') }}"
            alt="Sabre Armory Logo"
            class="h-18 w-auto mr-3"
        />
    </div>

    <!-- Role badge -->
    <div class="mb-8">
        <div class="text-xs text-gray-400 uppercase">Signed in as</div>
        <div class="mt-1 flex items-center text-sm font-medium">
             Managtuba Atilla
            <span
                class="ml-2 px-2 py-0.5 rounded bg-gray-700 text-xs font-semibold"
            >
                OFFICER
            </span>
        </div>
    </div>

    <!-- Navigation -->
    <nav class="flex-1 text-sm">
        <ul class="space-y-2">
            <li>
                <a
                    href="#"
                    @click.prevent="setView('dashboard')"
                    class="flex items-center gap-3 px-4 py-2 rounded hover:bg-gray-700 transition-colors duration-200"
                >
                    <!-- Home icon -->
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="w-5 h-5"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"
                        />
                    </svg>
                    Dashboard
                </a>
            </li>
            <li>
                <a
                    href="#"
                    @click.prevent="setView('catalog')"
                    class="flex items-center gap-3 px-4 py-2 rounded hover:bg-gray-700 transition-colors duration-200"
                >
                    <!-- Search icon -->
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="w-5 h-5"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M21 21l-4.35-4.35M17 11a6 6 0 1 0-12 0 6 6 0 0 0 12 0z"
                        />
                    </svg>
                    Catalog
                </a>
            </li>
            <li>
                <a
                    href="#"
                    @click.prevent="setView('requests')"
                    class="flex items-center gap-3 px-4 py-2 rounded hover:bg-gray-700 transition-colors duration-200"
                >
                    <!-- Mail icon -->
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="w-5 h-5"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M3 8l7.89 5.26a2 2 0 0 0 2.22 0L21 8m-18 8h18a2 2 0 0 0 2-2V6a2 2 0 0 0-2-2H3a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2z"
                        />
                    </svg>
                    Requests
                </a>
            </li>
            <li>
                <a
                    href="#"
                    @click.prevent="setView('transactions')"
                    class="flex items-center gap-3 px-4 py-2 rounded hover:bg-gray-700 transition-colors duration-200"
                >
                    <!-- Refresh icon -->
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="w-5 h-5"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M4 4v6h6M20 20v-6h-6M4 14a8 8 0 0 1 16 0"
                        />
                    </svg>
                    Issue / Return
                </a>
            </li>
            <li>
                <a
                    href="#"
                    @click.prevent="setView('inventory')"
                    class="flex items-center gap-3 px-4 py-2 rounded hover:bg-gray-700 transition-colors duration-200"
                >
                    <!-- Package icon -->
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="w-5 h-5"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M20 7l-8-4-8 4v10l8 4 8-4V7z"
                        />
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 3v18"
                        />
                    </svg>
                    Inventory
                </a>
            </li>
            <li>
                <a
                    href="#"
                    @click.prevent="setView('users')"
                    class="flex items-center gap-3 px-4 py-2 rounded hover:bg-gray-700 transition-colors duration-200"
                >
                    <!-- Users icon -->
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="w-5 h-5"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M17 20h5v-1a4 4 0 0 0-4-4h-1M9 20H4v-1a4 4 0 0 1 4-4h1M12 12a5 5 0 1 0 0-10 5 5 0 0 0 0 10z"
                        />
                    </svg>
                    Users
                </a>
            </li>
            <li>
                <a
                    href="#"
                    @click.prevent="setView('reports')"
                    class="flex items-center gap-3 px-4 py-2 rounded hover:bg-gray-700 transition-colors duration-200"
                >
                    <!-- Chart icon -->
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="w-5 h-5"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M11 3v18m4-6v6m-8-4v4"
                        />
                    </svg>
                    Analytics
                </a>
            </li>
            <li>
                <a
                    href="#"
                    @click.prevent="setView('audit')"
                    class="flex items-center gap-3 px-4 py-2 rounded hover:bg-gray-700 transition-colors duration-200"
                >
                    <!-- Clipboard icon -->
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="w-5 h-5"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M9 12h6m-6 4h6m2 4H7a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h3l2-2 2 2h3a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2z"
                        />
                    </svg>
                    Audit Logs
                </a>
            </li>
            <li>
                <a
                    href="#"
                    @click.prevent="setView('settings')"
                    class="flex items-center gap-3 px-4 py-2 rounded hover:bg-gray-700 transition-colors duration-200"
                >
                    <!-- Cog icon -->
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="w-5 h-5"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M12 8c-2.21 0-4 1.79-4 4s1.79 4 4 4 4-1.79 4-4-1.79-4-4-4zm0-6v2m0 16v2m10-10h-2M4 12H2m15.364-7.364l-1.414 1.414M6.05 17.95l-1.414 1.414m0-15.364l1.414 1.414M17.95 17.95l1.414 1.414"
                        />
                    </svg>
                    Settings
                </a>
            </li>
        </ul>
    </nav>

    <!-- Logout button -->
    <div class="mt-auto pt-6">
        <button
            class="w-full px-4 py-2 bg-red-600 rounded hover:bg-red-700 transition-colors duration-200"
        >
            Logout
        </button>
    </div>
</aside>
